<div>
  <h1>Discover all the lists:</h1>

<% @lists.each do |list| %>
  <%= link_to list_path(list) do %>
    <div><%= list.name %></div>
  <% end %>
<% end %>
</div>

<div><%= link_to "Add a new list", new_list_path %></div>
